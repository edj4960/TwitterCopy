export class user {
    
    constructor(profileImage, firstName, lastName, alias, password) {
        this.profileImage = profileImage
        this.firstName = firstName
        this.lastName = lastName
        this.alias = alias
        this.password = password
    }

    getUser(alias, password) {
        
    }
}